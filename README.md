# Private Publishing using Bitcoin

A Python implemented global commitment scheme.

## Details
To set up for testing you need to run setup() in the User folder and setup() in Server and use a mac with Python 3.

